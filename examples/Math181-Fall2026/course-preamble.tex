% course-preamble.tex — shared preamble for every Math 181 (Fall 2026) document.
%
% Auto-loaded at \AtEndPreamble by course-metadata.sty because coursemeta.tex in
% this directory sets  preamble-file = course-preamble. Put course-wide macros
% and settings here ONCE instead of repeating them in each document's preamble;
% a no-op for any course that doesn't set the key.

\newcommand{\ivt}{Intermediate Value Theorem}
