% pset-instructions.tex — default problem-set instructions.
%
% pset.cls \input's this file (boxed) whenever a problem set sets neither
% \meta{pset-instructions-file=...} nor an inline \meta{pset-instructions={...}}.
% Because it is a real file, SyncTeX inverse search works: double-clicking the
% instructions in the PDF jumps here (mirrors quiz/autoexam/bingo instructions).
%
% Edit the wording below to change the default for every problem set, or override
% per set in the document via:
%   \meta{pset-instructions-file = my-instructions}  % unboxed, full control
%   \meta{pset-instructions      = {short inline text}} % boxed, inline
Show all work for full credit. Box or circle final answers. Late submissions
lose 10\% per day.
