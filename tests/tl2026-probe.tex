% Temporary diagnostic for issue #69 -- NOT part of the library.
%
% v3. The first two rounds killed both starting hypotheses: tag_suspend:n and
% tag_resume:n are BOTH still present in tagpdf 1.0c (nothing silently became
% \relax), and a flat list took leftmargin=* without complaint. The real pset
% log shows the `*' itself as the offending token, so the sentinel IS the
% trigger -- it just needs the conditions the flat probe lacked.
%
% This round isolates which condition: the real accessible \DocumentMetadata
% (previous probes used a bare tagging=on), and {parts} NESTED inside an outer
% list, which is how pset-template reaches it (\begin{parts} inside
% \begin{problem}).
\DocumentMetadata{lang=en,tagging=on,tagging-setup={math/setup={mathml-AF,mathml-SE},table/header-rows=1},pdfstandard={ua-2,a-4f}}
\documentclass{article}

\usepackage{enumitem}

\newlist{parts}{enumerate}{4}
\setlist[parts]{label=\textbf{(\alph*)}, leftmargin=*, topsep=2pt, parsep=2pt}

\newlist{outerproblem}{enumerate}{2}
\setlist[outerproblem]{label=\arabic*., leftmargin=2em}

\begin{document}

\typeout{PROBE: ===== A: flat parts, leftmargin=* =====}
\begin{parts}
	\item flat first
	\item flat second
\end{parts}
\typeout{PROBE: ===== A survived =====}

\typeout{PROBE: ===== B: parts NESTED in a list, leftmargin=* =====}
\begin{outerproblem}
	\item Outer problem text.
	\begin{parts}
		\item nested first
		\item nested second
	\end{parts}
\end{outerproblem}
\typeout{PROBE: ===== B survived =====}

\end{document}
