# 2026-09-12 — university-of-missouri-columbia

**profile** — HERD rank 70. PR #199.

Complete except accessibility: geometry (1.5in left, 1in elsewhere), double
spacing, and both pages built from the Graduate School's 2025-05 specimens.
Accessibility is recorded as **Not published**.

Things a later pass must not rediscover:

- Mizzou states its figures **twice, identically** — once as policy
  (`/policy/body-format-for-thesis-or-dissertation/`) and once in the guide
  (`/academics/thesis-dissertation/formatting-guidelines/`). No conflict to
  resolve, unusually.
- **Mizzou requires an approval page and explicitly forbids signatures on it**:
  "The approval page in your thesis/dissertation should not have committee
  signatures." Typed names only, and the class's default signature rule is
  replaced.
- **The title-page date is the GRADUATION month and year, not the defence
  date** — stated in as many words — and the specimen sets it in capitals.
- The specimen draws its three horizontal rules at three slightly different
  widths; the prose never mentions the rules at all. Read as a Word artifact and
  normalised to one width. Do not "fix" this back.
- The specimen prefixes each committee name with "Professor". The profile does
  **not** add that prefix — it would be wrong for a non-professor and the class
  cannot tell. Put the wanted form into `\committeemember`.
- Accessibility: the formatting guide, both policy pages and "Technology & Your
  Submission" were all read and none mentions accessibility, tagging, alt text,
  WCAG, PDF/A or the ADA. The Technology page is where it would live, and it
  goes straight from "must be submitted in pdf format" to multimedia formats.
- The filed document must be a single file named exactly `research.pdf`, all
  lowercase.

Round of 2026-09-12 (ten institutions). Skipped as already in flight: the 29
`profile/...` branches open at the start of the round.
