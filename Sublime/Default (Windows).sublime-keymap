[
	// Default (Windows).sublime-keymap -- live copy from Packages/User/.
	// Deploy to Packages/User/.
	//
	// No bindings here: the live Ctrl+B / Ctrl+Shift+B bindings ship in the
	// native package (Sublime/texlib/Default (Windows).sublime-keymap).
	// Earlier experimental quiet/noisy-build patterns are in git history.
]
