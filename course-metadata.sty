%% course-metadata.sty -- compatibility wrapper (TeXLib)
%% ============================================================================
%% The package moved to texlib-coursemeta.sty. `course-metadata' is too generic a name for a
%% library to claim in a shared TEXMF tree.
%%
%% This wrapper exists so the rename costs nothing: \RequirePackage{course-metadata}
%% and \usepackage{course-metadata} keep working. Prefer texlib-coursemeta in new code.
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{course-metadata}[2026/08/31 v0.8.0 course-metadata wrapper over texlib-coursemeta (TeXLib)]
\DeclareOption*{\PassOptionsToPackage{\CurrentOption}{texlib-coursemeta}}
\ProcessOptions\relax
\RequirePackage{texlib-coursemeta}
\endinput
